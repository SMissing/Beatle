import Foundation

// Minimal stub for BundleHelper - audio functionality removed
struct BundleHelper {
    static func resolveBundledURL(named name: String) -> URL? {
        // Audio functionality removed - ready for new implementation
        // This could be reimplemented to handle bundled resource loading without audio processing
        return nil
    }
}
